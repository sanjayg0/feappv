# N.B.  It is necessary to make clean build first

feappvcopy:   
	(cd elements; cp *.f ../../build)
	(cd interpolation; cp *.f ../../build)
	(cd piegl; cp *.f ../../build)
	(cd plot; cp *.f ../../build)
	(cd program; cp *.f ../../build)
	(cd quadrature; cp *.f ../../build)
	(cd refinement; cp *.f ../../build)
	@@echo "--> FEAPpv IgA Build updated <--"
